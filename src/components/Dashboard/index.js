import { Box, Grid } from '@mui/material';
import React, { Fragment } from 'react';

function Dashboard() {
    return (
        <Box mt={2}>
            <h1>Hello World</h1>
        </Box>
    )
}

export default Dashboard;